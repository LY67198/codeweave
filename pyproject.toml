[project]
name = "codeweave-workspace"
version = "0.1.0"
description = "Multi-agent coding assistant workspace"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["backend", "cli"]

[tool.uv.sources]
codeweave = { workspace = true }
cw = { workspace = true }
